package com.demo.structure;

public class AstroCalculator {

    public int galacticAdd(int alpha, int beta) {
        return alpha + beta;
    }

    public boolean isOrbitStable(int rotationSpeed) {
        return rotationSpeed % 2 == 0;
    }
}
